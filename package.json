{
  "name": "@mood-tracker/root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "prisma": "pnpm --filter @mood-tracker/prisma run prisma",
    "generate": "pnpm --filter @mood-tracker/verdant run generate",
    "preflight": "pnpm --filter @mood-tracker/verdant run preflight"
  },
  "devDependencies": {
    "turbo": "^1.6.1"
  }
}
